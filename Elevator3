#include <stdio.h>
#include <stdlib.h>


struct LinkedList {
    int data;
    struct LinkedList* next;
};


int main (int arg, char ** argv)
{
    int currentfloor;
    int MaxFloor;
    int input;
    int output;


    printf("Please enter the max floors in this building: ");
    scanf("%d", &MaxFloor);
    printf("You entered %d", MaxFloor);

    int floors[MaxFloor];

    for(int i = 0; i < MaxFloor; i++)
    {
        if( i == 0){
            struct LinkedList* head = NULL;
            head = (struct LinkedList*)malloc(sizeof(struct LinkedList));
        } else if (i == (MaxFloor-1)){
            struct LinkedList* tail = NULL;
            tail = (struct LinkedList*)malloc(sizeof(struct LinkedList));
        }else
        {

        }
    }

    /* for(int i = 0; i < MaxFloor; i++)
    {
       if(i == 0){
           head->data = i;
        head->next;
        } else if(i == (MaxFloor - 1)){
            tail->data = i;
            tail->next = NULL;
        }else
        {
            I = i;
            I->
        }
    } */

    printf("please enter the floor you would like to go to");
    scanf("%d", input);
    printf("You entered %d", input);



    }
